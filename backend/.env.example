# ===================================
# WAF Console Backend Environment Variables
# ===================================
# 1. Copy this file to .env in the same directory
# 2. Replace all placeholder values with actual values
# 3. Never commit .env file (it's in .gitignore)

# ===================================
# Database Configuration
# ===================================
DB_USERNAME=root
DB_PASSWORD=your-password-here

# ===================================
# OAuth2 Configuration (Google)
# ===================================
# Get these values from: https://console.cloud.google.com/
# 1. Create a project or select existing one
# 2. Go to "APIs & Services" > "Credentials"
# 3. Create "OAuth 2.0 Client ID" (Web application)
# 4. Add authorized redirect URI: http://localhost:8081/login/oauth2/code/google
GOOGLE_CLIENT_ID=your-google-client-id-here
GOOGLE_CLIENT_SECRET=your-google-client-secret-here

# ===================================
# Security Configuration
# ===================================
# JWT Secret (256-bit key for JWT signing)
# Generate with: openssl rand -base64 32
JWT_SECRET=change-this-to-your-256-bit-secret-key-for-jwt-signing

# CORS allowed origins (comma-separated)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# ===================================
# Redis Configuration
# ===================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# ===================================
# Kafka Configuration
# ===================================
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
KAFKA_ENABLED=true

# ===================================
# Elasticsearch Configuration
# ===================================
ELASTICSEARCH_URIS=http://localhost:9200
ELASTICSEARCH_USERNAME=
ELASTICSEARCH_PASSWORD=
ELASTICSEARCH_ENABLED=true

# ===================================
# Monitoring & Tracing
# ===================================
TRACING_ENABLED=false

# ===================================
# Slack Notifications
# ===================================
# Get Webhook URL from: https://api.slack.com/messaging/webhooks
# 1. Create a Slack App: https://api.slack.com/apps
# 2. Enable "Incoming Webhooks"
# 3. Add a webhook to your workspace
# 4. Copy the webhook URL
SLACK_ENABLED=false
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL
