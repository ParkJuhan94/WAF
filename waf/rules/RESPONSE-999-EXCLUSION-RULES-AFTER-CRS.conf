# CRS가 전부 로딩된 “이후”에 실행될 커스텀 룰(탐지/차단/체인 등)

# === Example: 간단 커스텀 트래버설 탐지 (초기엔 감지만) ===
SecRule REQUEST_URI|ARGS "(?:\.\./|%2e%2e/)" \
  "id:1000050,phase:2,log,pass,t:urlDecodeUni,msg:'Path traversal seen',tag:'custom'"
